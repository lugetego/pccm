<form method="post" action="{{ path('app_tutor_delete', {'id': tutor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tutor.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash"></i> Borrar</button>
</form>
